{
  "name": "server",
  "version": "1.0.0",
  "main": "app.js",
  "type": "module",  
  "scripts": {
    "start": "node app.js",
    "build": "cd ../client && npm install && npm run build",
    "dev": "nodemon app.js",  
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "path": "^0.12.7",
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}